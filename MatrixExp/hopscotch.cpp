#include <bits/stdc++.h>
using namespace std;

const int maxn = 55;
int a[maxn][maxn],n,k;
pair<int,int> num[maxn*maxn];

signed main(){
    cin>>n>>k;
    for(int i = 0; i < n; i++){
        for(int j = 0; j < n;j++){
            cin>>a[i][j];
            num[a[i][j]].push_back[]
        }
    }

}
